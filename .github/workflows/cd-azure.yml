name: CD-Azure

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Azure login
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}
      - name: Build and push images
        run: |
          echo "Configure ACR push commands here"
          echo "docker build/push backend, frontend, rag-service"
      - name: Deploy container apps
        run: |
          echo "Configure az containerapp update commands here"

